# Retro — Epic 0 (Project Setup & Infrastructure)

**Date:** 2026-08-03
**Scope:** Epic 0 — TASK-001 through TASK-009
**Author:** Dev (jdugang)

## Delivered
- Boilerplate (pure shell: infra + UI shell on placeholder data) → baseline commit
- TASK-002 scaffold, TASK-006 Supabase clients — delivered by boilerplate, verified
- TASK-005 CI split into pr.yml + deploy.yml, guarded Vercel step
- TASK-008 initial schema migration (5 tables, constraints, 10 indexes) — applied to staging
- TASK-009 idempotent seed (4 leave types, 5 test profiles, balances)
- TASK-003/004/007 = manual DevOps (Supabase/Vercel/Resend) — done to training scope

## Findings

| # | Finding | Severity | Resolution | Action / Owner |
|---|---------|----------|------------|----------------|
| 1 | Task CSV shipped unquoted CHECK-constraint literals — `CHECK(role IN (employee,...))` fails at apply as a column reference | High | Quoted all literals during TASK-008 PLAN | Captured as rule in `knowledge/rules/coding-standards.md`. **Closed.** |
| 2 | ARCH ER marks `leaves.leave_type` / `leave_balances.leave_type` as FK→`leave_types(name)`, but task + PRD say plain TEXT | Med | Kept plain TEXT to match task AC; flagged in SQL comment | **Open — raise with Solutions Architect** to reconcile ARCH vs task |
| 3 | ARCH ER omits `leave_balances.deleted_at`, but FR-DATA-001 mandates soft-delete on all business tables | Med | Added `deleted_at` to honor FR-DATA-001 | **Open — raise with Solutions Architect** (ARCH ER internal inconsistency) |
| 4 | Freshly migrated tables have no grants — service-role SELECT returns `403 42501` even with valid key | High | Out of TASK-008 scope (DDL only) | **Open → epic-1 TASK-011** now annotated to add table-level GRANTs. Owner: Dev |
| 5 | `supabase db reset` starts from empty each run, so "reset twice" never exercises `ON CONFLICT` idempotency | Low | Verified seed idempotency by running seed twice in a `postgres:16` container instead | Captured in `knowledge/patterns/verify-migration-in-container.md`. **Closed.** |

## What worked well
- **EVAP's EVALUATE step earned its keep** — caught the CHECK-literal bug (#1) at the plan gate, before any SQL was written. Blindly copying the CSV DDL would have failed at apply.
- **Throwaway `postgres:16` container** verified constraints, indexes, FKs, and seed idempotency where the session had no Supabase CLI/`psql` — now a reusable pattern.

## Action items
- [ ] Raise findings #2 and #3 with the Solutions Architect (ARCH ↔ task/PRD reconciliation) — **Owner: Dev → SA**
- [ ] Implement GRANTs in epic-1 TASK-011 (CSV already annotated) — **Owner: Dev**